apiVersion: batch/v1
kind: CronJob
metadata:
  name: git-indexer
spec:
  # At minute 5 past hour 2 and every 4th hour from 8 through 23.
  schedule: "5 2,8-23/4 * * *"
  jobTemplate:
    spec:
      suspend: true
      template:
        metadata:
          labels:
            job: git-indexer
        spec:
          containers:
          - name: git-indexer
            image: ghcr.io/sloppycoder/git-commit-indexer:develop-20230622-234844-d575781 # {"$imagepolicy": "bbx-dev:git-indexer-develop"}
            # command: ["bin/sleep", "10000"]
            env:
            - name: GITLAB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: bbx-dev-secret
                  key: gitlab-token
            volumeMounts:
            - name: data
              mountPath: /data
          imagePullSecrets:
          - name: ghcr-cred
          restartPolicy: OnFailure
          volumes:
          - name: data
            persistentVolumeClaim:
              claimName: git-indexer-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: git-indexer-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
